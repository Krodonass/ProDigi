using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VacuumHandle : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject gameManger;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
